import styles from './project.module.css'

export const Project = () => {
    return (
        <section id='project' className={`con`}>
            <div className="inner">
                <h2>Project</h2>
            </div>
        </section>
    );
};
